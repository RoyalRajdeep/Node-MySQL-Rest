<!DOCTYPE html>
<html>
    <head>
        <title><%=title%></title>
        <link rel="stylesheet" href="/../css/bootstrap.min.css">
    </head>
    <body style="padding-top : 90px;">
    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                        aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="http://localhost:3000/api/index">Qunatinsti</a>
            </div>
            <div id="navbar" class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <li class="active"><a href="http://localhost:3000/api/showAll">Instruments</a></li>
                    <li><a href="http://localhost:3000/api/instrument-query">Execute Query</a></li>
                </ul>
            </div><!--/.nav-collapse -->
        </div>
    </nav>

    <div class="container">

      <div class="starter-template">
        <h1>List of Instruments</h1>
        <p class="lead">All you get is this text and a mostly barebones HTML document.</p>

        <div class="">
            <table class="table table-striped">
                <tr>
                    <th>No</th>
                    <th>Instrument Id</th>
                    <th>Position</th>
                    <th>Status</th>
                </tr>

                <% if(data.length){
                 for(var i = 0;i < data.length;i++) {
                %>

                <tr>
                    <td><%=(i+1)%></td>
                    <td><%=data[i].instrument_id%></td>
                    <td><%=data[i].position%></td>
                    <td><% if(data[i].position > 0){%>
                        Bought
                        <%
                        }else{ %>
                        Sold
                        <% } %></td>
                </tr>
            <% }

             }else{ %>
                 <tr>
                    <td colspan="4">No Data</td>
                 </tr>
            <% } %>

            </table>
        </div>

        <div class="page-data">
             <ul class="err-area"></ul>
        </div>

        <div class="row">
            <div class="col-md-12 alert alert-success">
                Page Automatically Refreshes in every <strong>5</strong> Seconds ...</a>.
            </div>
        </div>

      </div>
    </div>
    <script>
        setTimeout(function(){
            window.location.reload(1);
        }, 5000);
    </script>
</body>
</html>
